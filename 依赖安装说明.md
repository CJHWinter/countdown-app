# 📦 依赖安装说明

## 🚀 一键安装（推荐）

双击运行 **`安装依赖.bat`**

这个脚本会自动：
- ✅ 检测 Python 环境
- ✅ 检测 Node.js 环境
- ✅ 安装 Pillow (图标生成)
- ✅ 安装 Flask/yt-dlp (B站下载器)
- ⚠️  提示下载 FFmpeg

---

## 📋 依赖清单

### Python 依赖

| 包名 | 版本 | 用途 | 安装命令 |
|------|------|------|----------|
| **pillow** | >=10.0.0 | 图标生成 | `pip install pillow` |
| **flask** | >=2.3.0 | Web框架 | `pip install flask` |
| **flask-cors** | >=4.0.0 | 跨域支持 | `pip install flask-cors` |
| **yt-dlp** | >=2023.10.13 | B站下载 | `pip install yt-dlp` |
| **requests** | >=2.31.0 | HTTP请求 | `pip install requests` |

### Node.js 依赖

**无需额外依赖**（使用内置模块）

### 其他工具

| 工具 | 用途 | 下载地址 |
|------|------|----------|
| **FFmpeg** | 视频处理 | https://github.com/BtbN/FFmpeg-Builds/releases |

---

## 🔧 手动安装

### 方式一：使用 requirements.txt

```bash
# 安装 B站下载器依赖
cd 爬取B站原视频
pip install -r requirements.txt
cd ..

# 安装图标生成依赖
pip install pillow
```

### 方式二：逐个安装

```bash
# 图标生成
pip install pillow

# B站下载器
pip install flask flask-cors yt-dlp requests
```

---

## ⚠️ 常见问题

### Q1: pip 安装很慢

**A:** 使用国内镜像源：

```bash
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple pillow
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
```

### Q2: 找不到 pip 命令

**A:** 确保 Python 已正确安装：

1. 重新安装 Python
2. 安装时勾选 "Add Python to PATH"
3. 或使用：`python -m pip install pillow`

### Q3: FFmpeg 下载太慢

**A:** 手动下载：

1. 访问：https://github.com/BtbN/FFmpeg-Builds/releases
2. 下载：`ffmpeg-master-latest-win64-gpl.zip`
3. 解压到 `爬取B站原视频/` 目录
4. 确保路径正确：`爬取B站原视频/ffmpeg-master-latest-win64-gpl/bin/ffmpeg.exe`

### Q4: Node.js 安装后仍提示找不到

**A:** 

1. 关闭所有命令行窗口
2. 重新打开
3. 运行：`node --version`
4. 如果仍然不行，重启电脑

---

## ✅ 验证安装

### 验证 Python 依赖

```bash
python -c "import PIL; print('Pillow OK')"
python -c "import flask; print('Flask OK')"
python -c "import yt_dlp; print('yt-dlp OK')"
```

### 验证 Node.js

```bash
node --version
```

### 验证 FFmpeg

```bash
爬取B站原视频\ffmpeg-master-latest-win64-gpl\bin\ffmpeg.exe -version
```

---

## 📱 各功能依赖关系

| 功能 | 需要的依赖 |
|------|-----------|
| **生成PWA图标** | Python + Pillow |
| **生成音乐列表** | Node.js |
| **B站下载器** | Python + Flask + yt-dlp + FFmpeg |
| **本地服务器** | Python 或 Node.js |
| **倒计时应用** | 仅需浏览器（无需依赖） |

---

**最后更新**: 2025-11-07

